# Environment Variables Template
# Copy this file to .env and modify the values as needed

# Environment
ENVIRONMENT=development

# Flask Configuration
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=true
MAX_CONTENT_LENGTH=16777216

# Model Configuration
MODEL_CHECKPOINT_PATH=models/checkpoints/best_model.pth
MODEL_DEVICE=auto
MODEL_NUM_CLASSES=2

# Training Configuration
BATCH_SIZE=32
LEARNING_RATE=0.0001
NUM_EPOCHS=50

# Data Configuration
DATA_DIR=data
CSV_FILE=
NUM_WORKERS=2

# Logging Configuration
LOG_LEVEL=INFO
LOG_DIR=logs

# Security Configuration (DO NOT commit actual secret key)
SECRET_KEY=your-secret-key-here-change-this-in-production

# Optional: Database URL (if using database in future)
# DATABASE_URL=sqlite:///app.db

# Optional: External service configurations
# TENSORBOARD_LOG_DIR=runs
# SENTRY_DSN=your-sentry-dsn-here

# Optional: Cloud storage (if using cloud storage for models/data)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_S3_BUCKET=your-s3-bucket-name

# Optional: Email configuration (for notifications)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=your-email@gmail.com
# SMTP_PASSWORD=your-email-password